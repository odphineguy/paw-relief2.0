
import { SymptomType, TriggerType, ReminderType } from './types';

export const ALL_SYMPTOMS: SymptomType[] = Object.values(SymptomType);
export const ALL_TRIGGERS: TriggerType[] = Object.values(TriggerType);
export const ALL_REMINDER_TYPES: ReminderType[] = Object.values(ReminderType);
